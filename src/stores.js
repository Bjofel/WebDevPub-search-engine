import { writable } from 'svelte/store';


export const promise = writable();
export const isHomePage = writable(true)
export const bubbleSearch = writable(false)
export const timer = writable(false)
